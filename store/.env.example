# A Little Hill Farm — store secrets. NOT committed (.gitignore covers it).
#
# Upload all of these to the deployed Worker with:
#     cd store && npx wrangler secret bulk .env
#
# For local `wrangler dev`, copy this file to .dev.vars (also gitignored).
#
# Use the TEST-mode Stripe keys until the full checklist in README.md passes.

STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
ADMIN_PASSWORD=
